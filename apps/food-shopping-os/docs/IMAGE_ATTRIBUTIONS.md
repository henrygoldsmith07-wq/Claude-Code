# Recipe artwork

Recipe cards use deterministic inline SVG icons generated by
`src/data/recipe-images.js`. There are no third-party photographs, remote
image requests, or image licences required. Each icon is rendered at a
1024 x 680 viewBox and scales cleanly from a small card to a recipe hero.

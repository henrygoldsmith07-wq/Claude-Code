# Supabase project settings — required. Run supabase/schema.sql against your
# project, enable Auth email/password signups, then set these from the
# project's API settings. Never expose a service-role key to the browser.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
